import Logo from '../UI/Icons/Logo';
import '../../styles/Nav/NavLogo.scss';

const NavLogo = () => {
  return (
    <button className="nav-logo">
      <Logo />
    </button>
  );
};

export default NavLogo;
